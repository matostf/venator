# Local runtime data (lives on the Fly volume in production; backup kept locally).
data/

.env
.venv/
venv/
__pycache__/
*.pyc
.DS_Store

# Config local do Claude Code
.claude/

# Dumps de acervos externos clonados (ex.: external/walters-api ~43 MB, com .git próprio)
external/
